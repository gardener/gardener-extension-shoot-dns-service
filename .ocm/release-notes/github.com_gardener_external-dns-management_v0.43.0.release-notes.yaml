ocm:
  component_name: github.com/gardener/external-dns-management
  component_version: v0.43.0
release_notes:
- audience: operator
  author:
    hostname: github.com
    type: githubUser
    username: MartinWeindel
  category: other
  contents: '[next-generation] Adds a new `propagationWaitTime` configuration option
    (default: 10s) to `DNSEntryControllerConfig` that prevents phantom reads from
    authoritative DNS servers after a DNSEntries is deleted and immediately recreated
    with same spec.'
  mimetype: text/markdown
  reference: '[#907](https://github.com/gardener/external-dns-management/pull/907)'
  type: standard
- audience: operator
  author:
    hostname: github.com
    type: githubUser
    username: MartinWeindel
  category: other
  contents: '[next-generation] use the default finalizer `dns.gardener.cloud/compound`
    instead of `gardendns-next-gen.dns.gardener.cloud/compound`'
  mimetype: text/markdown
  reference: '[#912](https://github.com/gardener/external-dns-management/pull/912)'
  type: standard
- audience: operator
  author:
    hostname: github.com
    type: githubUser
    username: MartinWeindel
  category: bugfix
  contents: '[next-generation] Fix wildcard DNS records for alias targets with routing
    policy'
  mimetype: text/markdown
  reference: '[#915](https://github.com/gardener/external-dns-management/pull/915)'
  type: standard
